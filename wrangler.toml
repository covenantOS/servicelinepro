# Cloudflare Pages Configuration
# For GitHub-connected deploys, set these in the Cloudflare Pages dashboard:
#   Build command: npm run build
#   Build output directory: dist
#   Do NOT set a deploy command - Pages handles this automatically.

name = "servicelinepro"
compatibility_date = "2025-01-01"

# For Pages static assets
[assets]
directory = "./dist"
